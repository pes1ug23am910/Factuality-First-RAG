# Test data

`sample_wiki.jsonl` is an original synthetic fixture for deterministic index and
retrieval tests. It is not copied from Wikipedia or another dataset and must not
be treated as benchmark evidence.

`evaluator_sanity_v1.json` contains synthetic, known-outcome evaluator cases for
interface and fail-closed checks only. It does not establish scientific validity.
